# .dockerignore for simple build - allow webapp but exclude other large directories
test/
lib/
node_modules/
karma-reports/
.idea/
.tmp/
# Exclude the playwright directory as much as we can as the snapshots are huge and we bind mount it in
playwright/
!playwright/docker-entrypoint.sh
# Allow webapp directory for simple build
!webapp/